# برای نصب: pip install -r requirements.txt

# برای درخواست‌های HTTP همگام (synchronous) برای ارسال اعلان‌ها به Discord
requests==2.31.0
# برای زمان‌بندی اجرای دوره‌ای اسکریپت (هر ۵ دقیقه)
schedule==1.2.2
# برای سریال‌سازی و دی‌سریال‌سازی سریع JSON برای مدیریت کش و فایل‌های داده
orjson==3.10.7
# برای تجزیه محتوای HTML صفحات Medium
beautifulsoup4==4.12.3
# برای ارسال اعلان‌ها به تلگرام
python-telegram-bot==20.7
# برای درخواست‌های HTTP غیرهمزمان (آسنکرون) جهت اسکرپ کردن سریع‌تر
aiohttp==3.9.5
# برای مدیریت قفل فایل‌ها جهت جلوگیری از مشکلات دسترسی همزمان
filelock==3.15.4
# برای ترجمه پیام‌های اعلان به زبان‌های مختلف (مثل فارسی)
deep-translator==1.11.4
# برای لاگ‌گیری رنگی در کنسول جهت خوانایی بهتر
colorlog==6.8.2
# برای نمایش نوار پیشرفت هنگام اسکرپ کردن تگ‌های Medium
tqdm==4.66.5
# برای پشتیبانی از کش Redis (اختیاری، در صورت استفاده از سرور Redis)
redis==5.0.8
pytz
